# 末日突围 · ZOMBIE SIEGE

第一人称视角的末日丧尸生存射击小游戏。你被困在丧尸围城的废墟中，必须消灭 6 波尸潮，完成突围。

纯 HTML5 + Canvas 2D 实现，**无任何第三方依赖、无音频资源文件**。场景与音效程序化生成，第一视角武器使用一张透明 PNG 素材，双击 `index.html` 即可游玩。

## 运行方式

方式一：直接用浏览器打开 `index.html`。

方式二：启动本地静态服务器：

```bash
python3 -m http.server 8080
# 浏览器打开 http://localhost:8080
```

## 操作

| 按键 | 功能 |
| --- | --- |
| W A S D | 移动 |
| Shift | 奔跑 |
| 鼠标 | 瞄准 / 转向（开始游戏后自动锁定鼠标） |
| 鼠标左键 | 射击（可按住连射） |
| R | 换弹 |
| M | 开启 / 关闭声音 |
| Esc | 暂停并打开“退出本局”菜单 |

## 玩法

- 共 **6 波**尸潮，每波清空后恢复 **25 生命 + 30 弹药**，通关第 6 波即突围成功
- **爆头 3 倍伤害**：普通丧尸 1 枪爆头、2 枪躯干
- 游戏中每隔约 **9–16 秒**在玩家周围随机投放弹药箱（+30 弹药）或医疗包（+30 生命），地面常规补给最多保留 3 个
- 弹药完全耗尽且场上没有弹药箱时，系统会在 **2 秒内**保底投放弹药，避免战斗死锁
- 击倒丧尸仍有 **24% 概率**额外掉落弹药箱或医疗包
- 三种丧尸：
  - 🧟 普通丧尸 —— 跛行逼近
  - 🏃 迅捷丧尸 —— 第 2 波起出现，高速低血量
  - 💪 狂暴丧尸 —— 第 4 波起出现，高血量高伤害

## 项目结构

```
zombie-siege/
├── assets/
│   └── m4a1-first-person.png # 写实第一视角武器透明素材
├── index.html    # 页面结构与菜单/暂停/结算界面
├── src/
│   └── game.js   # 游戏引擎：渲染、物理、AI、音频、输入、界面逻辑
├── README.md
└── .gitignore
```

## 技术要点

- **伪 3D 透视投影**：yaw/pitch 相机模型 + 深度排序的 billboard 渲染，Canvas 2D 实现
- **Pointer Lock API**：第一视角瞄准，支持 Esc 暂停与重新锁定
- **混合渲染**：丧尸、废墟建筑、粒子与 HUD 由 Canvas 程序化绘制；M4A1 使用带透明通道的写实第一视角素材
- **多层武器反馈**：30 发自动射击、枪身后坐、抛壳、枪烟、方向性枪口火焰、镜头震动、动态准星与换弹位移
- **Web Audio API 程序化声景**：持续风声和低频氛围、脚步、分层枪声、机械换弹、丧尸低吼、受击与补给反馈均实时合成，无音频文件
- **游戏循环**：约 60fps 固定步长循环（`setInterval`，16ms），带时间步长钳制
- **Hitscan 命中判定**：准星射线与丧尸包围盒求交，按命中高度区分爆头/躯干

## 起源

本项目由 DeepSeek Harness 动态 Cordis 插件（`zomfps-1`）迁移而来：
插件版运行在对话页面内（启动面板 + 全屏浮层），本仓库为**独立可运行版本**，
界面由 React 改写为原生 DOM，Cordis timer 服务替换为 `setInterval`，其余游戏逻辑保持一致。

## 许可

MIT License —— 自由使用、修改与分发。
